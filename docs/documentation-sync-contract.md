# Documentation Sync Contract

After each push to `main`, automation may generate a narrow documentation update.
Only files in `docs/auto-sync/` may be rewritten automatically.
README files, SPEC files, contracts, and sentence docs still require human review.
